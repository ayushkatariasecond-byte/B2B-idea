# The existing Express/Prisma backend (same one the Expo dashboard talks to).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Used to build absolute URLs in sitemap.xml/robots.txt — set to the real deployed
# domain once this is live.
SITE_URL=http://localhost:3000
