# Working rules for this repo

Both files below are **standing instructions** — they apply to every task in this
repository, not just when invoked by name. They are imported rather than copied so the
skill files under `.claude/skills/` stay the single source of truth.

@.claude/skills/iq/SKILL.md

@.claude/skills/token/SKILL.md

When the two pull in opposite directions, `iq` rule 4 and `token` rule 12 win: brevity is
never worth an unverified claim. Cut narration and routine log noise, never the evidence
that a fix actually works.
